1.3.5 修复了一些Bug, 同时添加了一些小功能

在<commentGenerator>下添加如下配置, 两个配置共同对注释生成起作用, addRemarkComments为true则数据库表,字段注释会拼接出来
<property name="suppressAllComments" value="false"/>
<property name="addRemarkComments" value="true"/>

<plugin type="org.mybatis.generator.plugins.FluentBuilderMethodsPlugin" />
只要在<context>下的<property>结点后添加该配置, 就可以生成带有fluent风格的model代码。样例如下

public Person withId(Long id) {
	this.setId(id);
	return this;
}
public Person withUserId(Integer userId) {
	this.setUserId(userId);
	return this;
}

Person p = new Person();
p.withId(1).withName("name").withAge(5);

整体修改过程与 1.3.2 类似, 生成注释那里有些不同
